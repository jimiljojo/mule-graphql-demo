schema{
    query: Query
}

type Query{
    user(id: ID!) : [User]
    users(gender: String) : [User]
}

type User {
	id: ID
    name: String
    username: String
    email: String
    gender: String
}